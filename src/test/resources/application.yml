spring:
  #  datasource:
  #    url: jdbc:h2:tcp://localhost/~/datajpa
  #    username: sa
  #    password:
  #    driver-class-name: org.h2.Driver
  #
  jpa:
    #    hibernate:
    #      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

#logging.level:
#  org.hibernate.SQL: debug
server:
  port: 9000

# swagger를 off하기 위한 yml property값입니다.
springdoc:
  api-docs:
    enabled: false
